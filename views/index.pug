extends layout

block content
  h1= title
  p My name is sunil and I am a student, may not look like much But iam.
  button(type="button", class="btn btn-secondary", onclick="goFaster()") push me
  #faster
     p Increase the speed

  script. 
    var mph=20;
    var change=1;
    function goFaster()
    {
      mph=mph+change;
      change++;
      document.getElementById("faster").innerHTML= `MPH ${mph} with change ${change}`;
    }
